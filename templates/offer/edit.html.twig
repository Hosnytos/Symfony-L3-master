{% extends 'admin/layout.html.twig' %}

{% block title %}Modifier une offre{% endblock %}

{% block body %}
    <div class="row">
         <div class="col-md-12">
                 <div class="full">
                    <div class="heading_main text_align_center">
                        <h2><span class="theme_color"></span>Modification d'une offre</h2>
                    </div>
                 </div>
         </div>
    </div>

    {{ include('offer/_form.html.twig', {'button_label': 'Valider'}) }}

    <br>
    {{ include('offer/_delete_form.html.twig') }}

    <br>
    <a href="{{ path('offer_index') }}">Annuler</a>
    <br>
    <p></p>
{% endblock %}
