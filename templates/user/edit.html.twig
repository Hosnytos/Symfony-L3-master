{% extends 'admin/layout.html.twig' %}

{% block title %} Modifier le rôle  {% endblock %}

{% block body %}
    <div class="row">
         <div class="col-md-12">
                 <div class="full">
                    <div class="heading_main text_align_center">
                        <h2><span class="theme_color"></span>Modifier le rôle</h2>
                    </div>
                 </div>
         </div>
    </div>

    <p> Utilisateur - {{ user.email }} </p>

    {{ include('user/_form.html.twig', {'button_label': 'Valider'}) }}

    <br>
    <a href="{{ path('user_index') }}">Annuler</a>

{% endblock %}
