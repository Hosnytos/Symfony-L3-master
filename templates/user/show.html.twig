{% extends 'admin/layout.html.twig' %}

{% block title %}Utilisateurs - {{ user.email }} {% endblock %}

{% block body %}
    <div class="row">
         <div class="col-md-12">
                 <div class="full">
                    <div class="heading_main text_align_center">
                        <h2><span class="theme_color"></span>Utilisateur - {{ user.email }} </h2>
                    </div>
                 </div>
         </div>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Âge</th>
                <td>{{ user.age(user) }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Rôles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>
    
    <a class="btn btn-primary" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
    <br>

    <br>
    <a href="{{ path('user_index') }}">Retour</a>


{% endblock %}
