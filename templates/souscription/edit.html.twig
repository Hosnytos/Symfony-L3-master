{% extends 'espace-agent/layout.html.twig' %}

{% block title %}Modifier une souscription{% endblock %}

{% block body %}
    <div class="row">
         <div class="col-md-12">
                 <div class="full">
                    <div class="heading_main text_align_center">
                        <h2><span class="theme_color"></span>Modification d'une souscription</h2>
                    </div>
                 </div>
         </div>
    </div>

    {{ include('souscription/_form.html.twig', {'button_label': 'Valider'}) }}
    <br>

    {{ include('souscription/_delete_form.html.twig') }}
    <br>

    <a href="{{ path('souscription_index') }}">Annuler</a>
{% endblock %}
