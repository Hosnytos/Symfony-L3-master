{% extends 'admin/layout.html.twig' %}

{% block title %}Modifier une page d'accueil{% endblock %}

{% block body %}
    <div class="row">
         <div class="col-md-12">
                 <div class="full">
                    <div class="heading_main text_align_center">
                        <h2><span class="theme_color"></span>Modification une page d'accueil</h2>
                    </div>
                 </div>
         </div>
    </div>

    {{ include('homepage/_form.html.twig', {'button_label': 'Valider'}) }}

    <br>
    {{ include('homepage/_delete_form.html.twig') }}

    <br>
    <a href="{{ path('homepage_index') }}">Annuler</a>
    <br>
    <p></p>
{% endblock %}
